name: Deploy Lambda to AWS

on:
  push:
    branches:
      - main
  workflow_dispatch: # Permite execução manual

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  ECR_REPOSITORY_NAME: ${{ secrets.ECR_REPOSITORY_NAME }}

jobs:
  build-and-push:
    name: Build and Push Docker Image to ECR
    runs-on: ubuntu-latest
    
    outputs:
      ecr_image_uri: ${{ steps.vars.outputs.ecr_image_uri }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ff717079ee2060e4bcee96c4779b553acc87447c # v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@062b18b96a7aff071d4dc91bc00c4c1a7945b076 # v2
        with:
          registries: ${{ secrets.AWS_ACCOUNT_ID }}

      - name: Set up variables
        id: vars
        env:
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
        run: |
          # Valida que AWS_ACCOUNT_ID está disponível
          if [ -z "${AWS_ACCOUNT_ID}" ]; then
            echo "❌ Erro: AWS_ACCOUNT_ID não está disponível!"
            exit 1
          fi
          
          # Tenta obter o registry do output do login, se não estiver disponível, constrói manualmente
          ECR_REGISTRY="${{ steps.login-ecr.outputs.registry }}"
          
          if [ -z "${ECR_REGISTRY}" ] || [[ ! "${ECR_REGISTRY}" =~ ^[0-9]+\.dkr\.ecr\..+ ]]; then
            echo "⚠️ Registry não encontrado no output ou inválido, construindo manualmente..."
            ECR_REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
            echo "✅ Registry construído: ${ECR_REGISTRY}"
          else
            echo "✅ Registry obtido do login: ${ECR_REGISTRY}"
          fi
          
          echo "🔍 Debug - AWS_ACCOUNT_ID: ${AWS_ACCOUNT_ID}"
          echo "🔍 Debug - AWS_REGION: ${AWS_REGION}"
          echo "🔍 Debug - ECR_REGISTRY: ${ECR_REGISTRY}"
          echo "🔍 Debug - ECR_REPOSITORY_NAME: ${ECR_REPOSITORY_NAME}"
          
          ECR_REPOSITORY_URL="${ECR_REGISTRY}/${ECR_REPOSITORY_NAME}"
          IMAGE_TAG="sha-${GITHUB_SHA::7}"
          ECR_IMAGE_URI="${ECR_REPOSITORY_URL}:${IMAGE_TAG}"
          
          # Valida que a URI está completa
          if [[ ! "${ECR_IMAGE_URI}" =~ ^[0-9]+\.dkr\.ecr\..+ ]]; then
            echo "❌ Erro: ECR_IMAGE_URI construída está inválida: ${ECR_IMAGE_URI}"
            exit 1
          fi
          
          echo "🔍 Debug - ECR_REPOSITORY_URL: ${ECR_REPOSITORY_URL}"
          echo "🔍 Debug - IMAGE_TAG: ${IMAGE_TAG}"
          echo "🔍 Debug - ECR_IMAGE_URI: ${ECR_IMAGE_URI}"
          
          echo "ECR_REGISTRY=${ECR_REGISTRY}" >> "$GITHUB_ENV"
          echo "ECR_REPOSITORY_URL=${ECR_REPOSITORY_URL}" >> "$GITHUB_ENV"
          echo "IMAGE_TAG=${IMAGE_TAG}" >> "$GITHUB_ENV"
          echo "ECR_IMAGE_URI=${ECR_IMAGE_URI}" >> "$GITHUB_ENV"
          
          echo "ecr_repository_url=${ECR_REPOSITORY_URL}" >> "$GITHUB_OUTPUT"
          echo "image_tag=${IMAGE_TAG}" >> "$GITHUB_OUTPUT"
          echo "ecr_image_uri=${ECR_IMAGE_URI}" >> "$GITHUB_OUTPUT"
          
          echo "✅ Output definido: ecr_image_uri=${ECR_IMAGE_URI}"

      - name: Build Docker image
        id: build
        run: |
          docker build -t ${{ env.ECR_IMAGE_URI }} .
          echo "✅ Imagem buildada: ${{ env.ECR_IMAGE_URI }}"

      - name: Tag image as latest
        run: |
          docker tag ${{ env.ECR_IMAGE_URI }} ${{ env.ECR_REPOSITORY_URL }}:latest

      - name: Push image to ECR
        run: |
          docker push ${{ env.ECR_IMAGE_URI }}
          docker push ${{ env.ECR_REPOSITORY_URL }}:latest

      - name: Verify job output
        run: |
          echo "🔍 Verificando output do job..."
          echo "Output do step vars: ${{ steps.vars.outputs.ecr_image_uri }}"
          echo "Output do job será: ${{ steps.vars.outputs.ecr_image_uri }}"
          if [ -z "${{ steps.vars.outputs.ecr_image_uri }}" ]; then
            echo "⚠️ Aviso: Output do step vars está vazio!"
            echo "Variável de ambiente ECR_IMAGE_URI: ${ECR_IMAGE_URI}"
          else
            echo "✅ Output do job está definido corretamente"
          fi

  update-lambda-image:
    name: Atualizar Imagem do Lambda
    needs: build-and-push
    runs-on: ubuntu-latest
    strategy:
      matrix:
        lambda_mode: [Customer, Admin, Migrator]
      fail-fast: false
    
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ff717079ee2060e4bcee96c4779b553acc87447c # v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Configurar nome do Lambda por modo
        id: lambda-config
        env:
          LAMBDA_MODE: ${{ matrix.lambda_mode }}
          # Permitir override via secrets se necessário
          LAMBDA_FUNCTION_NAME_CUSTOMER: ${{ secrets.LAMBDA_FUNCTION_NAME_CUSTOMER }}
          LAMBDA_FUNCTION_NAME_ADMIN: ${{ secrets.LAMBDA_FUNCTION_NAME_ADMIN }}
          LAMBDA_FUNCTION_NAME_MIGRATOR: ${{ secrets.LAMBDA_FUNCTION_NAME_MIGRATOR }}
        run: |
          LAMBDA_MODE="${{ matrix.lambda_mode }}"
          LAMBDA_MODE_UPPER=$(echo "${LAMBDA_MODE}" | tr '[:lower:]' '[:upper:]')
          
          # Valores padrão baseados no modo
          case "${LAMBDA_MODE_UPPER}" in
            CUSTOMER)
              DEFAULT_FUNCTION_NAME="fiap-fase4-infra-auth-lambda"
              ;;
            ADMIN)
              DEFAULT_FUNCTION_NAME="fiap-fase4-infra-auth-admin-lambda"
              ;;
            MIGRATOR)
              DEFAULT_FUNCTION_NAME="fiap-fase4-infra-auth-migrator-lambda"
              ;;
            *)
              echo "❌ Erro: Modo Lambda inválido: ${LAMBDA_MODE}"
              exit 1
              ;;
          esac
          
          # Construir nome da variável de override baseado no modo
          FUNCTION_NAME_VAR="LAMBDA_FUNCTION_NAME_${LAMBDA_MODE_UPPER}"
          
          # Obter valor da variável de ambiente (override) ou usar padrão
          LAMBDA_FUNCTION_NAME=$(eval echo \$${FUNCTION_NAME_VAR})
          if [ -z "${LAMBDA_FUNCTION_NAME}" ]; then
            LAMBDA_FUNCTION_NAME="${DEFAULT_FUNCTION_NAME}"
            echo "ℹ️ Usando nome padrão para ${LAMBDA_MODE}: ${LAMBDA_FUNCTION_NAME}"
          else
            echo "ℹ️ Usando nome customizado para ${LAMBDA_MODE}: ${LAMBDA_FUNCTION_NAME}"
          fi
          
          echo "🔍 Modo Lambda: ${LAMBDA_MODE}"
          echo "🔍 Lambda Function Name: ${LAMBDA_FUNCTION_NAME}"
          
          echo "lambda_function_name=${LAMBDA_FUNCTION_NAME}" >> "$GITHUB_OUTPUT"

      - name: Obter URI da imagem ECR
        id: setup-ecr-uri
        env:
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
          ECR_REPOSITORY_NAME: ${{ secrets.ECR_REPOSITORY_NAME }}
        run: |
          echo "🔍 Debug - Outputs do job build-and-push:"
          ECR_URI="${{ needs.build-and-push.outputs.ecr_image_uri }}"
          echo "ecr_image_uri (direto): ${ECR_URI}"
          echo ""
          echo "🔍 Todos os outputs disponíveis (JSON):"
          echo "${{ toJSON(needs.build-and-push.outputs) }}"
          echo ""
          
          # Valida se a URI está completa (deve começar com número do account)
          if [ -n "${ECR_URI}" ] && [[ "${ECR_URI}" =~ ^[0-9]+\.dkr\.ecr\..+ ]]; then
            echo "✅ ECR_URI válida encontrada: ${ECR_URI}"
            echo "ECR_IMAGE_URI=${ECR_URI}" >> "$GITHUB_ENV"
            echo "ecr_image_uri=${ECR_URI}" >> "$GITHUB_OUTPUT"
          else
            echo "⚠️ ECR_URI está vazia, inválida ou incompleta do output do job!"
            echo "URI recebida: '${ECR_URI}'"
            echo "Tentando construir manualmente..."
            # Constrói manualmente como fallback
            if [ -z "${AWS_ACCOUNT_ID}" ]; then
              echo "❌ Erro: AWS_ACCOUNT_ID não está disponível!"
              exit 1
            fi
            if [ -z "${ECR_REPOSITORY_NAME}" ]; then
              echo "❌ Erro: ECR_REPOSITORY_NAME não está disponível!"
              exit 1
            fi
            IMAGE_TAG="sha-${GITHUB_SHA::7}"
            ECR_URI="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPOSITORY_NAME}:${IMAGE_TAG}"
            echo "ECR_IMAGE_URI=${ECR_URI}" >> "$GITHUB_ENV"
            echo "ecr_image_uri=${ECR_URI}" >> "$GITHUB_OUTPUT"
            echo "✅ ECR_URI construído manualmente: ${ECR_URI}"
          fi
          
          # Validação final antes de continuar
          FINAL_URI="${ECR_IMAGE_URI:-${ECR_URI}}"
          if [[ ! "${FINAL_URI}" =~ ^[0-9]+\.dkr\.ecr\..+ ]]; then
            echo "❌ Erro crítico: ECR_IMAGE_URI final está inválida: ${FINAL_URI}"
            echo "Formato esperado: <account-id>.dkr.ecr.<region>.amazonaws.com/<repo>:<tag>"
            exit 1
          fi
          echo "✅ Validação final: ECR_IMAGE_URI=${FINAL_URI}"

      - name: Verificar Lambda existe e é do tipo Image
        env:
          LAMBDA_FUNCTION_NAME: ${{ steps.lambda-config.outputs.lambda_function_name }}
        run: |
          echo "🔍 Verificando Lambda antes de atualizar imagem..."
          echo "  - Lambda Function Name: ${LAMBDA_FUNCTION_NAME}"
          
          # Verifica se o Lambda existe na AWS
          if ! aws lambda get-function \
            --function-name "${LAMBDA_FUNCTION_NAME}" \
            --region "${AWS_REGION}" > /dev/null 2>&1; then
            echo "❌ ERRO CRÍTICO: Lambda '${LAMBDA_FUNCTION_NAME}' NÃO existe na AWS!"
            echo "   O Lambda deve ser criado no projeto principal antes de atualizar a imagem"
            echo "   Falhando o workflow para evitar tentativa de criação"
            exit 1
          fi
          
          # Verifica se o Lambda é do tipo Image (não Zip)
          PACKAGE_TYPE=$(aws lambda get-function \
            --function-name "${LAMBDA_FUNCTION_NAME}" \
            --region "${AWS_REGION}" \
            --query 'Configuration.PackageType' \
            --output text 2>/dev/null)
          
          if [ "${PACKAGE_TYPE}" != "Image" ]; then
            echo "❌ ERRO CRÍTICO: Lambda '${LAMBDA_FUNCTION_NAME}' não é do tipo Image!"
            echo "   PackageType atual: ${PACKAGE_TYPE}"
            echo "   Este workflow só funciona com Lambdas do tipo Image (container)"
            echo "   Falhando o workflow"
            exit 1
          fi
          
          # Mostra informações do Lambda
          CURRENT_IMAGE_URI=$(aws lambda get-function \
            --function-name "${LAMBDA_FUNCTION_NAME}" \
            --region "${AWS_REGION}" \
            --query 'Configuration.ImageUri' \
            --output text 2>/dev/null)
          
          echo "✅ Lambda existe e é do tipo Image (container)"
          echo "  - Image URI atual: ${CURRENT_IMAGE_URI}"
          echo "  - PackageType: ${PACKAGE_TYPE}"

      - name: Verificar se imagem existe no ECR
        env:
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
          ECR_REPOSITORY_NAME: ${{ secrets.ECR_REPOSITORY_NAME }}
        run: |
          echo "🔍 Verificando se a imagem existe no ECR..."
          IMAGE_TAG="sha-${GITHUB_SHA::7}"
          IMAGE_URI="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/${ECR_REPOSITORY_NAME}:${IMAGE_TAG}"
          echo "Verificando imagem: ${IMAGE_URI}"
          
          # Verifica se a imagem existe no ECR
          if aws ecr describe-images \
            --repository-name "${ECR_REPOSITORY_NAME}" \
            --image-ids imageTag="${IMAGE_TAG}" \
            --region "${AWS_REGION}" > /dev/null 2>&1; then
            echo "✅ Imagem encontrada no ECR: ${IMAGE_URI}"
          else
            echo "❌ Erro: Imagem não encontrada no ECR: ${IMAGE_URI}"
            echo "Verificando tags disponíveis..."
            aws ecr describe-images \
              --repository-name "${ECR_REPOSITORY_NAME}" \
              --region "${AWS_REGION}" \
              --query 'imageDetails[*].imageTags' \
              --output table || true
            exit 1
          fi

      - name: Atualizar imagem do Lambda
        env:
          LAMBDA_FUNCTION_NAME: ${{ steps.lambda-config.outputs.lambda_function_name }}
          ECR_IMAGE_URI: ${{ env.ECR_IMAGE_URI }}
        run: |
          echo "🔄 Atualizando imagem do Lambda..."
          echo "  - Lambda Function Name: ${LAMBDA_FUNCTION_NAME}"
          echo "  - Nova Image URI: ${ECR_IMAGE_URI}"
          
          # Valida URI antes de atualizar
          if [[ ! "${ECR_IMAGE_URI}" =~ ^[0-9]+\.dkr\.ecr\..+ ]]; then
            echo "❌ Erro: ECR_IMAGE_URI está em formato inválido: ${ECR_IMAGE_URI}"
            exit 1
          fi
          
          # Atualiza apenas a imagem do Lambda usando AWS CLI
          # Isso NÃO mexe no estado do Terraform do projeto principal
          echo ""
          echo "📋 Executando: aws lambda update-function-code"
          UPDATE_OUTPUT=$(aws lambda update-function-code \
            --function-name "${LAMBDA_FUNCTION_NAME}" \
            --image-uri "${ECR_IMAGE_URI}" \
            --region "${AWS_REGION}" \
            --output json 2>&1)
          UPDATE_EXIT_CODE=$?
          
          if [ ${UPDATE_EXIT_CODE} -eq 0 ]; then
            echo "✅ Imagem do Lambda atualizada com sucesso!"
            echo ""
            echo "📊 Informações da atualização:"
            echo "${UPDATE_OUTPUT}" | jq -r '. | "  - Function Name: \(.FunctionName)\n  - Image URI: \(.Code.ImageUri)\n  - Last Update Status: \(.LastUpdateStatus)\n  - State: \(.State)"' || echo "${UPDATE_OUTPUT}"
            
            # Aguarda a atualização completar
            echo ""
            echo "⏳ Aguardando atualização completar..."
            aws lambda wait function-updated \
              --function-name "${LAMBDA_FUNCTION_NAME}" \
              --region "${AWS_REGION}"
            
            echo "✅ Lambda atualizado e pronto para uso"
          else
            echo "❌ Erro ao atualizar imagem do Lambda"
            echo "Output:"
            echo "${UPDATE_OUTPUT}"
            exit 1
          fi

      - name: Verificar atualização do Lambda
        env:
          LAMBDA_FUNCTION_NAME: ${{ steps.lambda-config.outputs.lambda_function_name }}
        run: |
          echo "🔍 Verificando informações finais do Lambda após atualização..."
          echo ""
          
          LAMBDA_INFO=$(aws lambda get-function \
            --function-name "${LAMBDA_FUNCTION_NAME}" \
            --region "${AWS_REGION}" \
            --output json 2>/dev/null)
          
          if [ -n "${LAMBDA_INFO}" ]; then
            echo "📊 Informações do Lambda na AWS:"
            echo "${LAMBDA_INFO}" | jq -r '.Configuration | "  - Function Name: \(.FunctionName)\n  - Function ARN: \(.FunctionArn)\n  - Image URI: \(.ImageUri)\n  - Last Modified: \(.LastModified)\n  - State: \(.State)\n  - Package Type: \(.PackageType)"'
            echo ""
            echo "✅ Lambda atualizado com sucesso!"
          else
            echo "⚠️ Não foi possível obter informações do Lambda"
          fi
