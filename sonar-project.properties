# Configuração do SonarQube/SonarCloud
# Este arquivo é lido automaticamente pelo SonarScanner

# Exceção para regra S4790 (JWT secret keys) no arquivo TokenService.cs
# Justificativa: O secret JWT em produção sempre vem de variáveis de ambiente configuradas no Lambda.
# O fallback para configuração é apenas para desenvolvimento local (appsettings.Development.json não é versionado).
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=csharpsquid:S4790
sonar.issue.ignore.multicriteria.e1.resourceKey=**/Services/TokenService.cs

